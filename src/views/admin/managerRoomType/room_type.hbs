<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{#if roomType}}Edit{{else}}New{{/if}} Room Type</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.tiny.cloud/1/your-api-key/tinymce/6/tinymce.min.js" referrerpolicy="origin"></script>
    
</head>

<body>
    <div class="container mt-5">
        <h1>{{#if roomType}}Edit{{else}}New{{/if}} Room Type</h1>
        <a href="/admin/roomsType" class="btn btn-secondary mb-3">Back to Room Types</a>
        <form action="{{#if roomType}}/admin/roomsType/edit/{{roomType._id}}{{else}}/admin/roomsType/new{{/if}}" method="POST">
            <div class="mb-3">
                <label for="type" class="form-label">Type:</label>
                <input type="text" id="type" name="type" class="form-control" value="{{roomType.type}}" required
                    minlength="3" maxlength="50">
            </div>
            <div class="mb-3">
                <label for="description" class="form-label">Description:</label>
                <textarea id="description" name="description" class="form-control" required minlength="10"
                    maxlength="500">{{roomType.description}}</textarea>
            </div>
            <button type="submit" class="btn btn-primary">{{#if roomType}}Update{{else}}Create{{/if}}</button>
        </form>
    </div>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/js/bootstrap.min.js"></script>
</body>

</html>