<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quản lý Khách sạn</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        .container {
            display: flex;
        }

        .sidebar {
            width: 25%;
            padding: 20px;
            background-color: #f4f4f4;
            border-right: 1px solid #ddd;
            margin-top: 20px;
        }

        .content {
            width: 75%;
            padding: 20px;
        }

        h2 {
            margin-top: 0;
        }

        .filter-group {
            margin-bottom: 20px;
        }

        .filter-group label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
            color: #333;
        }

        .filter-group select,
        .filter-group input[type="range"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
            font-size: 1em;
            background-color: #fff;
            transition: border-color 0.3s;
        }

        .filter-group select:focus,
        .filter-group input[type="range"]:focus {
            border-color: #007BFF;
        }

        button {
            display: block;
            width: 100%;
            padding: 10px;
            background-color: #007BFF;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1em;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #0056b3;
        }

        .hotel-list {
            display: flex;
            flex-direction: column;
        }

        .hotel {
            display: flex;
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ddd;
            background-color: #fff;
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .hotel:hover {
            transform: scale(1.05);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
        }

        .hotel img {
            width: 400px;
            height: 266px;
            object-fit: cover;
            margin-right: 10px;
        }

        .hotel-details {
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            flex-grow: 1;
        }

        .hotel-name {
            font-size: 1.2em;
            font-weight: bold;
        }

        .hotel-price {
            color: #007BFF;
            font-size: 1em;
        }

        .hotel-standard {
            text-align: center;
            background-color: #f0f0f0;
            padding: 5px;
            border-radius: 5px;
            width: 50px;
            margin: 0 auto;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="sidebar">
            <h2>Bộ lọc</h2>
            <form>
                <div class="filter-group">
                    <label for="city">Thành phố</label>
                    <select id="city" name="city">
                        <option value="all">Tất cả</option>
                        <option value="hanoi">Hà Nội</option>
                        <option value="hcmc">Hồ Chí Minh</option>
                        <!-- Thêm các tùy chọn khác nếu cần -->
                    </select>
                </div>
                <div class="filter-group">
                    <label for="standard">Tiêu chuẩn</label>
                    <select id="standard" name="standard">
                        <option value="all">Tất cả</option>
                        <option value="5">5 sao</option>
                        <option value="4">4 sao</option>
                        <option value="3">3 sao</option>
                        <!-- Thêm các tùy chọn khác nếu cần -->
                    </select>
                </div>
                <div class="filter-group">
                    <label for="priceRange">Giá tiền (0$ - 10000$)</label>
                    <input type="range" id="priceRange" name="priceRange" min="0" max="10000" value="5000"
                        oninput="updatePriceValue(this.value)">
                    <div>Giá hiện tại: <span id="priceValue">5000</span>$</div>
                </div>
                <button type="submit">Áp dụng</button>
            </form>
        </div>
        <div class="content">
            <h2>Danh sách Khách sạn</h2>
            <div class="hotel-list">
                {{#each allHotels}}
                <div class="hotel">
                    <img src="../public/img/hotel-1.jpg" alt="{{this.hotelName}}">
                    <div class="hotel-details">
                        <div class="hotel-name">{{this.hotelName}}</div>
                        <div class="hotel-standard">{{this.hotelStandard}} sao</div>
                        <div class="hotel-price">{{this.hotelPrice}}$</div>
                    </div>
                </div>
                {{/each}}
            </div>
        </div>
    </div>
    <script>
        function updatePriceValue(value) {
            document.getElementById('priceValue').innerText = value;
        }

    </script>
</body>

</html>